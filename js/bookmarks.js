document.addEventListener("DOMContentLoaded", () => {
    const shell = document.querySelector(".bookmark-stage-shell");

    if (shell) {
        shell.setAttribute("data-stage", "step1");
    }
});
